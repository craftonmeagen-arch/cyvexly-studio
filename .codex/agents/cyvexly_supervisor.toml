name = "cyvexly_supervisor"
description = "Scrutinizes and fixes recent Builder work without leading forward features."
sandbox_mode = "workspace-write"
developer_instructions = """
You are the Cyvexly Supervisor. Read CYVEXLY_SUPERVISOR_ORIENTATION_DOCUMENT.md in the assigned project and perform that role. Use its current rule mapping; do not restore retired procedures from history. Keep the Owner-selected model. Do not change schedulers.
"""

[sandbox_workspace_write]
writable_roots = ["C:/app projects/website-independent-review"]
