# Cyvexly Auditor Debt

- `CYV-IFA-005` — **Owner Decision / Priority Before Deployment:** confirm public domain, set `metadataBase`, rebuild, and verify static OG/Twitter URLs no longer point to localhost; retain preview no-index until launch.
- `CYV-IFA-006` — **Owner Decision / Priority Before Deployment:** choose an approved server-side email/storage path and implement automatic Planner receipt/confirmation with consent, error, retry, and retention behavior.
- `/about`, `/privacy`, `/terms` — project debt requiring Owner founder/jurisdiction facts; current 404s are intentional and not new findings.
- `CYV-IFA-007` — provisional closure: scratch route absent in current `src`; recheck after the next immutable snapshot/build.
- `CYV-IFA-008` — **Verified closed in IFA-2026-08-31-R9:** invalid dynamic `/services/<slug>` and `/work/<slug>` routes return HTTP 404 and hydrate with the custom `Page not found — Cyvexly Studio` title/body; valid dynamic routes remain 200. R10 did not repeat this pass. Reopen only if a later accepted source regresses this behavior.
- `CYV-IFA-009` — **P2 / Launch Quality, mitigated:** the Planner form is gated behind restore readiness and saved-draft values restore without the R8 jump/input-loss path. Closure still requires a true no-draft first-use proof on a clean Browser origin/context; this round could not isolate it without prohibited storage clearing or policy-blocked alternate origins.
- `CYV-IFA-010` — **P1 / Launch Quality, open:** the sitewide `.site-root > :not(.site-atmosphere)` rule overrides the direct `SiteHeader`'s authored `sticky top-0 z-50` with computed `position: relative; z-index: 1`. R14 reproduced the header scrolling out of view on desktop and phone. Preserve the header's position/z-index while retaining the atmosphere stacking layer, then re-run terminal-scroll and overlay checks.
- `CYV-IFA-011` — **P1 / Accessibility, open:** R17 measured Contact field-level error text at 12px `--color-warning-coral` (`rgb(217,67,95)`) over the frosted input background (`rgb(248,251,255)`) at `4.1139:1`, below WCAG AA's `4.5:1` normal-text threshold. The shared Planner field-error components use the same token. Darken the error color or strengthen the error field treatment, then recheck composited error/focus states.
- Unconfirmed: public deployment re-check from this sandbox, physical keyboard, reduced-motion preference, valid external mail, real integrations, and cross-browser behavior.
- R11 note: the accepted Home cleanup is now audited locally. Public/production adoption, reduced-motion/save-data emulation, and physical keyboard remain unconfirmed; no new finding was assigned.
- R12 note: Planner restore-ready status and service-entry draft precedence were observed locally. The public Render URL and alternate `127.0.0.1` origin were Browser-policy blocked; an existing draft prevented true no-draft closure, so `CYV-IFA-009` remains partially mitigated.
- R13 note: site-wide blue-glass atmosphere and fixed-token contrast screened positively across representative local pages, but final composited-pixel WCAG proof, Owner acceptance, public adoption, and production metadata remain unconfirmed. Home preview still emits localhost OG/Twitter URLs and no canonical (`CYV-IFA-005`).
- R14 note: exact local runtime confirmed `CYV-IFA-010`; no public Render check was possible under the saved Browser permission boundary. The shared atmosphere, route shells, dynamic 404, phone containment, and menu still passed their sampled checks.
- R17 note: exact local Contact/FAQ runtime confirmed `CYV-IFA-011`; validation semantics, FAQ behavior, route handoff, containment, atmosphere semantics, and Browser diagnostics passed. No valid submit or external side effect was attempted. Builder lock and mutable source were preserved.
