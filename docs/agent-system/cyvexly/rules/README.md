# Team Two Website — current imported rule set

These shared packets govern the **Team Two Website** roles for the Cyvexly
Studio product through the project mapping one directory above.

These eight Markdown packets are preserved unchanged from the supplied files,
verified against the NEW AI BUILD RULES Google Drive folder on 2026-09-05.
SYNC_MANIFEST.json records source IDs, dates, sizes and installed SHA-256 hashes.
Do not edit these files to adapt another product's examples. Use the Cyvexly
role mapping one directory above. Replace the verified packet only when the
Owner authorizes a new source revision. There is no automatic sync scheduler.

Older root draft packets were removed; Git history is not active authority.
