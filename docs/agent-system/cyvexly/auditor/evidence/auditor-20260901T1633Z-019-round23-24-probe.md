# IFA-2026-09-01-R19 — Round 23/24 contrast, glass, and focus probe

## Custody and scope

- Review: `IFA-2026-09-01-R19` / `auditor-20260901T1633Z-019`
- Heartbeat minute zero: `2026-09-01T16:33:57.340Z`
- Immutable accepted product snapshot: `06fbadd6448b4e8e8c77d1618b1c4e6cb14238f2` (`fix: focus the first invalid contact field`); `git rev-parse 06fbadd:src` = `d9f3e4a976e08f4acc11307b152bcb388d6885a6`
- Exact role-owned runtime: `C:\Users\Tcraf\AppData\Local\Temp\cyvexly-auditor-20260901T1633Z-019`, served on `5273`; dependency junction points only to the existing workspace `node_modules`.
- A live Builder lock for `cyvexly-builder-20260901T160800Z-r24` was present and preserved. During this pass the mutable branch advanced beyond the accepted snapshot to `2cd9121`; those later files were not inspected.
- The standard guard was attempted and failed with managed-permission `UnauthorizedAccessException` writing `.codex/role-state/auditor.active.json`; the exact archive/runtime deviation is recorded here and in the report.

## Methods and evidence

The real Codex in-app Browser was used against the isolated runtime. This is a new method/surface relative to R18: I exercised Contact validation/focus, FAQ disclosure, Work filtering, and a broad route-family glass sweep rather than repeating the prior Home/Services sticky-only check. At `1440×1000`, I inspected `/contact`, `/faq`, `/pricing`, `/process`, `/services`, `/work`, and `/work/aurora-spaces`; at `390×844` and `320×844`, I repeated the route-family containment/header checks. I opened exact IAB captures for Contact desktop, Pricing full-page, Aurora Spaces, and the focused Contact error state at phone width.

## Results

### Round 23 warning-token correction — pass; `CYV-IFA-011` closes

On `/contact`, clicking **Send message** with every field empty produced the summary alert and four field errors. The first invalid control received focus (`activeElement#name`, `name`), and each invalid control remained associated with its field error through `aria-describedby` (`name-error`, `email-error`, `message-error`, `consent-error`). The revised shared token computes to `rgb(189, 45, 73)` (`#bd2d49`) at 12px field-error text and 14px summary text. Relative-luminance checks against the observed frosted/content baselines were `5.5660:1` (`#f8fbff`), `5.3002:1` (`#ebf7fe`), and `5.3311:1` (`#eff7fc`), all above the normal-text AA threshold of `4.5:1`. The phone focused-error capture shows the visible focus ring and all field-level messages without clipping.

### Round 24 full-height blue glass — pass on sampled route family

All sampled routes loaded with one `main`, one `h1`, and the intended pale-blue architectural field. The new shared `main` background carries grid/traces/radial depth; ordinary sections compute to translucent blue glass (`rgba(223, 241, 253, 0.54)`, `backdrop-filter: blur(12px) saturate(1.26)`), continuation bands to `rgba(225, 242, 253, 0.72)`, and protected glass sections to `rgba(216, 238, 252, 0.78)`. Dark cyber focal CTA/hero panels remain visually distinct in the opened Pricing and Aurora captures. No sampled route fell back to the prior flat-white treatment.

### Responsive/interaction checks — pass

- Requested `390×844` gives a `375px` layout width and `documentElement.scrollWidth=375`; requested `320×844` gives a `305px` layout width and `scrollWidth=305`. No horizontal overflow was observed on the seven route family members at either width.
- The header remains `position: sticky; top: 0; z-index: 50` at desktop and phone widths. At 320px, opening the compact menu expands the sticky header to `377px`; the visible menu is inside that header, and after scrolling the header remains at viewport `y=0`.
- FAQ disclosure opens with `aria-expanded="true"` and a mounted answer. Work `Commerce` filtering sets its chip `aria-pressed="true"` and leaves only `/work/vellora-care` visible.
- HTTP smoke: `/contact`, `/faq`, `/pricing`, `/process`, `/services`, `/work`, and `/work/aurora-spaces` returned 200; `/about` returned the intentional 404. Browser warning/error logs were empty for every sampled route.

## Verification limits

`eslint src` passed with exit `0`. `next build` with Turbopack was blocked by the disposable runtime's out-of-root dependency junction; `next build --webpack` was blocked by managed HTTPS `EACCES` while fetching Google Fonts. These are environment/build-proof limits, not a browser runtime failure. Public Render adoption, physical keyboard hardware, reduced-motion/Safari/Firefox behavior, and real external mail delivery remain unconfirmed.

## Evidence files

- `auditor-20260901T1633Z-019-runtime-metrics.json`
- `auditor-20260901T1633Z-019-contact.png`
- `auditor-20260901T1633Z-019-pricing.png`
- `auditor-20260901T1633Z-019-work-aurora.png`
- `auditor-20260901T1633Z-019-contact-390.png`
- `auditor-20260901T1633Z-019-contact-390-error-viewport.png`
